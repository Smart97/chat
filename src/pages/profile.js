export const ProfilePage = () => {
/* 
    const {isVisibleProfile, firstname, lastname, ...profile} = useSelector(
        (state) => {
            return state.profile;
        }
    )

    return (
        <div>
            <h1>Profile</h1>
            {isVisibleProfile && (
                <div>
                    <h2>firstname: {firstname}</h2>
                    <h2>lastname: {lastname}</h2>
                    <h2>phone: {phone}</h2>
                </div>
            )}
            <button onClick={() => dispatch(togleVisibileProfile())}>togleVisibileProfile</button>
            <ProfileForm firstname={firstname} lastname={lastname} {...profile}></ProfileForm>
        </div>
    ) */
    
    return (
        <div>
            <h1>Profile</h1>
        </div>)
}